{
  "$schema": "https://raw.githubusercontent.com/anthropics/mcpb/main/schemas/mcpb-manifest-v0.4.schema.json",
  "manifest_version": "0.4",
  "name": "openalex-mcp",
  "display_name": "OpenAlex (bibliograph)",
  "version": "2.1.2",
  "description": "OpenAlex: works, authors, sources, institutions and citation relations across the global scholarly graph, with typed envelopes and query receipts.",
  "long_description": "One of the six bibliograph MCP servers (https://github.com/ckgerteis/bibliograph-mcp). Every search answers with one typed JSON envelope: the term actually sent and its script, how the source matched it, how broad the set is, typed diagnostics, a receipt hash, and the provider's attribution. Receipts can be deposited to an append-only, hash-chained ledger so a search behind a footnote can be named and re-run. Needs no Python of its own: Claude Desktop builds the bundle's environment with uv (one on the PATH if present, otherwise a copy the app downloads) when the extension is installed (a Python 3.10 or later already on the machine is used; one is downloaded only if there is none; roughly 40 MB of libraries), and every launch reuses it and takes under a second.",
  "author": {
    "name": "Christopher Gerteis",
    "email": "christopher.gerteis@gmail.com",
    "url": "https://www.christophergerteis.net"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/ckgerteis/openalex-mcp.git"
  },
  "homepage": "https://github.com/ckgerteis/openalex-mcp",
  "documentation": "https://github.com/ckgerteis/openalex-mcp#readme",
  "support": "https://github.com/ckgerteis/openalex-mcp/issues",
  "license": "MIT",
  "keywords": [
    "bibliographic-search",
    "bibliometrics",
    "citation-graph",
    "digital-humanities",
    "mcp",
    "model-context-protocol",
    "openalex",
    "query-receipts",
    "scholarly-metadata"
  ],
  "server": {
    "type": "uv",
    "entry_point": "main.py",
    "mcp_config": {
      "command": "uv",
      "args": [
        "run",
        "--directory",
        "${__dirname}",
        "--frozen",
        "main.py"
      ],
      "env": {
        "OPENALEX_API_KEY": "${user_config.openalex_api_key}",
        "OPENALEX_EMAIL": "${user_config.openalex_email}",
        "MCP_RECEIPT_DIR": "${user_config.receipts_dir}",
        "MCP_RECEIPT_SESSION": "${user_config.receipt_session}"
      }
    }
  },
  "user_config": {
    "openalex_api_key": {
      "type": "string",
      "title": "OpenAlex API key",
      "description": "Optional but recommended: free from https://openalex.org/settings/api. OpenAlex meters keyless access by a per-IP daily budget.",
      "sensitive": true,
      "required": false
    },
    "openalex_email": {
      "type": "string",
      "title": "Contact email (legacy mailto)",
      "description": "Optional. Sent only when no key is set; OpenAlex retired the polite pool in February 2026.",
      "sensitive": true,
      "required": false
    },
    "receipts_dir": {
      "type": "directory",
      "title": "Receipts folder",
      "description": "Where this server deposits its append-only, hash-chained query receipts (one <server>.jsonl per server). Leave empty to run without receipts; the envelope will then say RECEIPT_NOT_DEPOSITED.",
      "required": false
    },
    "receipt_session": {
      "type": "string",
      "title": "Receipt session slug",
      "description": "A project or article slug stamped on every receipt, e.g. my-article-2026. Optional.",
      "required": false
    }
  },
  "compatibility": {
    "claude_desktop": ">=0.10.0",
    "platforms": [
      "darwin",
      "linux",
      "win32"
    ],
    "runtimes": {
      "python": ">=3.10"
    }
  }
}
